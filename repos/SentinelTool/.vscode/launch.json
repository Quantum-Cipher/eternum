{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Swift CLI (built binary)",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/.build/debug/${workspaceFolderBasename}"
    }
  ]
}
